// MyRealization Component
// is placed at the bottom of the message component. It allows users to Add or Remove a comment about what that insight means to them or helped them to realize.
// This component receives life lesson data as an array and emits selections to the parent component.

// import { shallowMount } from "@vue/test-utils";
// import LifeLessonsOracle from "@/components/LifeLessonsOracle.vue";

describe("LifeLessonsDisplay.vue", () => {
  it("renders the Lesson Display component properly", () => {
    // const wrapper = shallowMount(Lesson Display, {});
  });

  it("displays the reset button properly", () => {
    // const wrapper = shallowMount(LifeLessonsOracle, {});
  });

  it("displays the added comments properly", () => {});

  it("displays the Add button properly ", () => {});

  it("adds the comment when the Adde button is clicked", () => {});

  it("displays the Remove button properly ", () => {});

  it("deletes the comment when the Remove button is clicked", () => {});
});
